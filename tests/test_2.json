{
  "name": "health_check",
  "description": "GET /health → status=ok, model_loaded=true",
  "method": "GET",
  "path": "/health",
  "expected_status": 200,
  "expected_body": {
    "status": "ok",
    "model_loaded": true
  }
}
