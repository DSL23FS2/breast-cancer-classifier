# Docker Hub credentials (needed for CI/CD push)
DOCKERHUB_USER=your_dockerhub_username
DOCKERHUB_PASS=your_dockerhub_password

# PostgreSQL password (v2.0+; moved to Vault in v3.0)
# DB_PASSWORD=changeme
